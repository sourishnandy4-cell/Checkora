import { Chess } from 'chess.js';
import fs from 'fs';

// Read lessons.ts and parse it (since it's TS, we'll just extract the object using regex or eval if possible, but let's just write a simple parser or compile it)
// Alternatively, since I'm in a JS script, I can just use ts-node or transpile it.
// Let's create a temporary index.mjs that imports it.
